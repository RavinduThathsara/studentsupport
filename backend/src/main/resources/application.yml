server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_support?createDatabaseIfNotExist=true
    username: root
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  jwt:
    # IMPORTANT: use a long random string (at least 32 chars)
    secret: "CHANGE_ME_TO_A_LONG_RANDOM_SECRET_KEY_32+"
    expirationMillis: 86400000  # 1 day
